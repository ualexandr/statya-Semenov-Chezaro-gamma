% Уважаемые Авторы!
%Для подготовки pdf-файлы Вашей работы необходимо
%запускать компиляцию с помощью команды latex
%(в программе WinEdt это делается нажатием
%клавиш Shift + Ctrl + L), после этого нужно
%из получившегося dvi файла сделать pdf
%с помощью команды dvi2pdf (в программе WinEdt для
%этого есть специальная кнопка в верхней панели).
%Не нужно использовать команду pdflatex,
%так как она препятствует команде \mag,
%которая производит увеличение страниц
%статьи для удобства редактирования.
%Просим не использовать в тексте рукописи
%нестандартные макроопределения.


\documentclass{article}


\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[english,russian]{babel}
\usepackage[tbtags]{amsmath}
\usepackage{amsfonts,amssymb}

\hoffset -10mm
\voffset -7mm


\usepackage{mathrsfs}
\usepackage[short]{mz2ewin}
\usepackage{graphicx}
\let\rom=\textup

\theoremstyle{plain}

\newtheorem{condition}{Условие}
\newtheorem{lemma}{Лемма}
\newtheorem{theorem}{Теорема}
\newtheorem{proposition}{Предложение}
\newtheorem{corollary}{Следствие}
\newtheorem{notation}{Обозначения}
\newtheorem{definition}{Определение}

\theoremstyle{definition}
\newtheorem{proof}{Доказательство}\def\theproof{}
\newtheorem{remark}{Замечание}


\overfullrule5pt
\mag1300
\begin{document}

\udk{517.521.2}

\date{\\
Исправленный вариант\\ }

\author{Н.\,Н.~Авдеев}
\address{
	Воронежский государственный университет,
	г.~Воронеж
}
\email{avdeev@math.vsu.ru, nickkolok@mail.ru}


\title{
	О пространстве почти сходящихся последовательностей
}



\maketitle
\begin{fulltext}

%\begin{abstract}

%\end{abstract}

\begin{keywords}
Почти сходящаяся последовательность, критерий Лоренца, ограниченная последовательность.
\end{keywords}


\footnotetext{Работа выполнена в Воронежском университете при поддержке РНФ, грант 16-11-10125.}

Через $\ell_\infty$ будем обозначать пространство ограниченных последовательностей с обычной нормой,
через $c$ и $c_0$~--- пространства сходящихся и сходящихся к нулю последовательностей соотв.,
через $\mathbb{N}$~--- множество натуральных чисел,
через $T$~--- оператор сдвига влево, т.е.
$T(x_1,x_2,...) = (x_2,x_3,...)$.


Изучая множество банаховых пределов, Лоренц \cite{Lorentz}
ввёл пространство почти сходящихся последовательностей $ac$.
Последовательность $x\in\ell_\infty$ называется почти сходящейся,
если существует такое число $t$, что
\begin{equation}
\label{eq:intro_ac}
	\lim_{n\to\infty} \frac{1}{n} \sum_{k=m+1}^{m+n} x_k = t
\end{equation}
равномерно по $m$.
Если $t=0$, то говорят, что последовательность
принадлежит пространству $ac_0$ почти сходящихся к нулю последовательностей.
Легко видеть, что $x\in ac_0$ тогда и только тогда, когда
\begin{equation}\label{crit_pos_ac0}
	\forall(B  \in\mathbb{N})
	\exists(n_0\in\mathbb{N})
	\exists(m_0\in\mathbb{N})
	\forall(n\geq n_0)
	\forall(m\geq m_0)
	\\
	\left[
		\left|
		\frac{1}{n}
		\sum_{k=m+1}^{m+n} x_k
		\right|
		<
		\frac{1}{B}
	\right]
	.
\end{equation}
Удобство соотношения \eqref{crit_pos_ac0} в
выборе $m_0$ в зависимости от $B$.
В \cite[теорема 7 и следствие 12]{Alekhno_ac0} доказано, что пространство $ac_0$ замкнуто, недополняемо в $\ell_\infty$ и несепарабельно.
В силу соотношения $ac = ac_0 \oplus \mathbb{R}^1$ этими же тремя свойствами обладает $ac$.

В первой части настоящей статьи приводится критерий почти сходимости неотрицательной последовательности к нулю,
основанный на идеях \cite[\S 5]{SemSukUsa14}.
Во второй части изучается для почти сходящихся последовательностей связь $\alpha$--функции [TODO] с расстоянием до пространства $c$
и приводится критерий сходимости почти сходящейся последовательности, основанный на $\alpha$--функции.



\begin{lemma}
Пусть $n_i$~--- строго возрастающая последовательность чисел из $\mathbb{N}$,
\begin{equation}
	x_k = \left\{\begin{array}{ll}
		1, & \mbox{~если~} k = n_i
		\\
		0  & \mbox{~иначе.~}
	\end{array}\right.
\qquad
	M(j) = \liminf_{i\to\infty} (n_{i+j} - n_i),
\end{equation}
Для того, чтобы $x\in ac_0$,
необходимо и достаточно, чтобы
\begin{equation}\label{lim_M(j)/j}
	\lim_{j \to \infty} {M(j)}/{j} = +\infty
	.
\end{equation}
\end{lemma}

\paragraph{Необходимость}
Понятно, что $M(j)\in\mathbb{N}$
и для любого $j$ существует лишь конечное количество отрезков из $M(j)+1$ элементов,
содержащих более $j+1$ единиц.
Кроме того, существует бесконечное количество отрезков из $M(j)+1$ элементов,
содержащих ровно $j+1$ единиц.
Отбросив от каждого отрезка конец, получим бесконечное количество отрезков из $M(j)$ элементов,
содержащих ровно $j$ единиц.
Через $E_j$ будем обозначать конец последнего отрезка из $M(j)+1$ элементов,
содержащего более $j+1$ единиц.

%\begin{proof}
Предположим противное, т.е.
\begin{equation}
	\liminf_{j \to \infty} {M(j)}/{j} = q < +\infty
	.
\end{equation}
Очевидно, $q>0$.
Найдётся счётное множество
$J\subset\mathbb{N}$ такое, что
$
	\forall(j\in J)\left[q \leq {M(j)}/{j} \leq Q \right],
$
где $Q$~--- наименьшее натуральное число, превосходящее $q$.
Для любого $j\in J$ существует бесконечно много отрезков из $jQ$ элементов,
на каждом из которых не менее $j$ единиц.
%
Т.к. $x\in ac_0$, то
\begin{equation}\label{Lorencz_ac0_epsilon}
	\forall(\varepsilon>0)
	\exists(n_0\in\mathbb{N})
	\forall(n \geq n_0)
	\forall(m\in\mathbb{N})
	\left[
		\frac{1}{n} \sum_{k=m+1}^{m+n}x_k < \varepsilon
	\right]
	.
\end{equation}

Положим $\varepsilon = 1/(Q+1)$ и отыщем $n_0$, удовлетворяющее \eqref{Lorencz_ac0_epsilon}.
Положим $n\in J$, $n\geq n_0$.
Такое $n$ всегда найдётся, т.к. $J$ счётно и $J\subset\mathbb{N}$.
Выберем $m$ так, чтобы отрезок из $nQ$ элементов,
содержащий не менее $n$ единиц,
начинался с $(m+1)$-го элемента.
Тогда
\begin{equation}
	\frac{1}{nQ}\sum_{k=m+1}^{m+nQ}x_k
	\geq
	\frac{1}{nQ} \cdot n
	=
	\frac{1}{Q}
	>
	\frac{1}{Q+1}
	=
	\varepsilon,
\end{equation}
что противоречит \eqref{Lorencz_ac0_epsilon}.
%Полученное противоречие  доказательство необходимости.

\paragraph{Достаточность}

Из равенства \eqref{lim_M(j)/j} вытекает, что
\begin{equation}\label{lim_M(j)/j_infty_def}
	\forall(C  \in\mathbb{N})
	\exists(j_0\in\mathbb{N})
	\forall(j \geq j_0)
	\left[
		{(M(j)+1)}/{(j+1)}>C
	\right]
	.
\end{equation}
Покажем, что выполнено
\eqref{crit_pos_ac0}.
Действительно, зафиксируем $B$.
Используя \eqref{lim_M(j)/j_infty_def} и приняв $C=2B$,
отыщем $j_0$ такое, что для любого $j \geq j_0$ выполнено
$M(j)+1>2B(j+1)$.
Положим $n_0 = 2B(j_0+1)$
и
$$
	m_0 = 2+\max_{1\leq j \leq j_0+1} E_j
	.
$$

Тогда для любых $m\geq m_0$ и $n\geq n_0$ имеем
\begin{multline*}
	\frac{1}{n} \sum_{k=m+1}^{m+n} x_k
	<
	\frac{1}{n} \left( \frac{n}{M(j_0)+1} + 1 \right) (j_0 + 1)
	=
	\frac{j_0+1}{M(j_0)+1} + \frac{j_0+1}{n}
	<%\leq
	\frac{1}{2B} + \frac{j_0+1}{n_0}
	=
	%\frac{1}{2B} + \frac{1}{2B}
	%=
	\frac{1}{B}
	,
\end{multline*}
т.е. условие критерия \eqref{crit_pos_ac0} выполнено
и $x\in ac_0$,
что и требовалось доказать.
%\end{proof}

Определим нелинейный оператор $\lambda$--срезки $A_\lambda$
для $x = (x_1, x_2, ...)\in\ell_\infty$ как
\begin{equation}
	(A_\lambda x)_k = \begin{cases}
		1, & \mbox{~если~} x_k \geq \lambda
		\\
		0  & \mbox{~иначе.~}
	\end{cases}
\end{equation}

\begin{theorem}
Пусть $x\in\ell_\infty$, $x\geq 0$.
Тогда
$
	x\in ac_0
$
если и только если
для любого $\lambda > 0$
выполнено
$
	A_\lambda x \in ac_0
$
.
\end{theorem}

\paragraph{Необходимость}
Пусть $x\in ac_0$.
Зафиксируем $\lambda > 0$.
Пусть $y=A_\lambda x$, тогда
\begin{equation}
	(\lambda y)_k = \begin{cases}
		\lambda, & \mbox{~если~} x_k \geq \lambda
		\\
		0  & \mbox{~иначе~}
	\end{cases}
\end{equation}
Таким образом, $0 \leq \lambda y \leq x$.
Следовательно, если $x \in ac_0$,
то $\lambda y \in ac_0$ и $y \in ac_0$.%,
%что и требовалось доказать.

\paragraph{Достаточность}
Случай $x=0$ тривиален.
Пусть $x\neq 0$.
Т.к.
$
	x\in ac_0
$
тогда и только тогда, когда
$
	{x}/{\|x\|}\in ac_0
$,
то будем полагать $\|x\|\leq 1$.
Более того,
$
	A_\lambda x \in ac_0
$
если и только если
$% \Leftrightarrow
	(1-\lambda)A_\lambda x \in ac_0
	.
$

Предположим противное, т.е. $x\notin ac_0$,
но $\forall(\lambda>0)[(1-\lambda)A_\lambda x \in ac_0]$.
Тогда
\begin{equation}\label{ac0_lambda_Lorencz_neg}
	\exists(\varepsilon_0 > 0)
	\forall(n_0 \in \mathbb{N})
	\exists(n > n_0)
	\exists(m \in \mathbb{N})
	\left[
		\frac{1}{n}\sum_{k=m+1}^{m+n} x_k \geq \varepsilon_0
	\right]
	.
\end{equation}
Найдём $\varepsilon_0$, удовлетворяющее \eqref{ac0_lambda_Lorencz_neg}.
Положим
$
	\varepsilon = \min\left\{ {\varepsilon_0}/{2}, {1}/{2} \right\}
	.
$
Легко видеть, что
\begin{equation}\label{ac0_lambda_Lorencz_neg_epsilon}
	\forall(n_0 \in \mathbb{N})
	\exists(n > n_0)
	\exists(m \in \mathbb{N})
	\left[
		\frac{1}{n}\sum_{k=m+1}^{m+n} x_k > \varepsilon
	\right]
	.
\end{equation}
%(знак неравенства стал строгим).
Построим последовательность
$
	y = \left( 1 - {\varepsilon}/{2} \right) A_{\varepsilon/2} x
	.
$
Заметим, что $y\in ac_0$, т.е. в силу \eqref{eq:intro_ac}
\begin{equation}\label{ac0_lambda_Lorencz}
	\forall(\varepsilon_1 > 0)
	\exists(n_1 \in \mathbb{N})
	\forall(n' > n_1)
	\forall(m' \in \mathbb{N})
	\left[
		\frac{1}{n}\sum_{k=m+1}^{m+n} y_k < \varepsilon_1
	\right]
	.
\end{equation}

Возьмём в \eqref{ac0_lambda_Lorencz} $\varepsilon_1 = \varepsilon/2$
и отыщем $n_1$.
Примем в \eqref{ac0_lambda_Lorencz_neg_epsilon}
$n_0 = n_1$ и отыщем $n$ и $m$.
Положим в \eqref{ac0_lambda_Lorencz} $n' = n$, $m' = m$.
Тогда по \eqref{ac0_lambda_Lorencz_neg_epsilon} и \eqref{ac0_lambda_Lorencz}
соответственно
\begin{equation}
	\frac{1}{n}\sum_{k=m+1}^{m+n} x_k > \varepsilon
	\qquad
	\mbox{и}
	\qquad
	\frac{1}{n}\sum_{k=m+1}^{m+n} y_k < \varepsilon/2
	.
\end{equation}
Вычитая, получим
\begin{equation}
	\frac{1}{n}\sum_{k=m+1}^{m+n} (x_k - y_k) > \varepsilon/2
	.
\end{equation}
Если среднее арифметическое чисел вида $x_k - y_k$ больше $\varepsilon/2$,
то существует хотя бы один индекс $k$ такой, что $x_k - y_k > \varepsilon/2$.
%
Предположим, что $k$ таково, что $x_k < \varepsilon/2$.
Тогда $y_k = 0$ и $x_k - y_k < \varepsilon/2$.
Значит, предположение неверно и $x_k \geq \varepsilon/2$.
Тогда $y_k = 1-\varepsilon/2$ и с учётом $\|x\|\leq 1$ имеем
$
	x_k - y_k \leq 1- y_k = 1 - (1-\varepsilon/2) = \varepsilon/2
	.
$
Следовательно, требуемого индекса $k$ не существует,
и \eqref{ac0_lambda_Lorencz_neg} не выполнено.
%Полученное противоречие завершает доказательство.
%\endproof

Сформулируем теперь этот критерий в терминах функций $M^{(\lambda)}(j)$.

\begin{theorem}
Пусть $x\in\ell_\infty$, $x \geq 0$, $\lambda>0$.
Обозначим через $n^{(\lambda)}_i$ возрастающую последовательность
индексов таких элементов $x$, что $x_k \geq \lambda$ тогда и только тогда,
когда $k=n^{(\lambda)}_i$ для некоторого $i$.
Обозначим
\begin{equation}
	M^{(\lambda)}(j) = \liminf_{i\to\infty} n^{(\lambda)}_{i+j} - n^{(\lambda)}_i
	.
\end{equation}


Тогда для того, чтобы $x\in ac_0$, необходимо и достаточно, чтобы
для любого $\lambda>0$
\begin{equation}
	\lim_{j \to \infty} {M^{(\lambda)}(j)}/{j} = +\infty
	.
\end{equation}
\end{theorem}


\begin{remark}
Требование произвольности $\lambda$ существенно.
Например, для $x_k = 1$ и $\lambda = 2$ имеем $A_\lambda x = 0 \in ac_0$,
но $x\notin ac_0$.
\end{remark}
Рассмотрим функцию, определяемую как
\begin{equation}
	\alpha(x) = \limsup_{i\to\infty} \max_{i \leq j \leq 2i} |x_i-x_j|
	.
\end{equation}

Пусть $\rho(x,c)$ и $\rho(x,c_0)$~--- расстояния от $x$ до пространств $c$
и $c_0$ соотв.
Из условия Липшица на $\alpha$--функцию
и того, что для $x\in c$
$\alpha(x)=0$ следует

\begin{lemma}
\label{thm:alpha_x_leq_2_rho_x_c}
	Для любого $x\in\ell_\infty$
	$
		\alpha(x) \leq 2\rho(x, c)
	$.
\end{lemma}

Эта оценка точна.
Как выясняется, верна и противоположная оценка.

\begin{lemma}
\label{thm:rho_x_c_leq_alpha_t_s_x}
	Для любого $x\in ac$ и для любого натурального $s$ верно
	$
		\rho(x,c)\leq \alpha(T^s x)
	$,
%	где $T$~--- оператор сдвига влево, т.е.
%	$T(x_1,x_2,...) = (x_2,x_3,...)$.
\end{lemma}
\begin{proof}
Зафиксируем $s$.
Пусть $x\in ac$, $\alpha(T^s x)=\varepsilon$.
Т.к. $x\in ac$, то в силу \eqref{eq:intro_ac} существует такое число $t$,
что
\begin{equation}
	\forall(p\in\mathbb{N})
	\exists(n_p \in\mathbb{N})\forall(n>n_p)\forall(m\in\mathbb{N})
	\left[
		\left|
			\frac{1}{n}\sum_{k=m+1}^{m+n} x_k
			-t
		\right|
		<\frac{\varepsilon}{p}
	\right]
	.
\end{equation}

Так как
\begin{equation}
	\alpha(T^s x) = \limsup_{k\to\infty} \max_{k<j\leq 2k-s} |x_k-x_j| = \varepsilon
	,
\end{equation}
то
\begin{equation}
	\forall(p\in\mathbb{N})
	\exists(k_p \in\mathbb{N})\forall(k>k_p)
	\forall(j: k< j \leq 2k-s)
	\left[
		|x_k - x_j|<\varepsilon + {\varepsilon}/{p}
	\right]
	.
\end{equation}
Положив $q_p = \max\{n_p, k_p\}$, получим, что для любого $q>q_p$
среднее арифметическое чисел $x_q$, $x_{q+1}$, ... , $x_{2q-s}$ отличается от $t$
не более, чем на $\varepsilon/p$, причём разница любых двух из этих чисел меньше $\varepsilon + \varepsilon/p$.
Следовательно, любое из этих чисел отличается от $t$ менее, чем на $\varepsilon + 2\varepsilon/p$.
В частности,
$
	|x_q - t| < \varepsilon + {2\varepsilon}/{p}
$.
Таким образом,
\begin{equation}
	\forall(p\in\mathbb{N})
	\exists(q_p \in\mathbb{N})
	\forall(q>q_p)
	\left[
		|x_q - t| < \varepsilon + {2\varepsilon}/{p}
	\right]
	,
\end{equation}
откуда немедленно следует, что $\rho(x,c) \leq \varepsilon$.
\end{proof}

Из лемм \ref{thm:alpha_x_leq_2_rho_x_c} и \ref{thm:rho_x_c_leq_alpha_t_s_x},
а также соотношения $\alpha(T^s x)\leq \alpha(x)$
незамедлительно следует
\begin{theorem}
%\label{thm:rho_x_c_leq_alpha_t_s_x}
	Для любого $x\in ac$
	\begin{equation}
		\frac{1}{2} \alpha(x) \leq \rho(x,c)\leq \lim_{s\to\infty} \alpha(T^s x) \leq \alpha(x)
		.
	\end{equation}
\end{theorem}

\begin{corollary}
	Для любого $x\in ac_0$
	\begin{equation}
		\frac{1}{2} \alpha(x) \leq \rho(x,c_0)\leq \lim_{s\to\infty} \alpha(T^s x) \leq \alpha(x)
		.
	\end{equation}
\end{corollary}
Все оценки точны, т.е. на некоторых элементах достигается равенство.
\begin{corollary}
	Пусть $x\in ac$.
	$x\in c$ тогда и только тогда, когда $\alpha(x) = 0$.
\end{corollary}

\begin{remark}
	Условие $x\in ac$ в лемме \ref{thm:rho_x_c_leq_alpha_t_s_x} существенно.
	Так, в \cite{VZMS} для любого $p\in\mathbb{N}$ строится элемент $x=Sy\notin ac$,
	для которого $\alpha(x) = 1/p$ и $\rho(x,c)=1/2$.
\end{remark}

Автор выражает благодарность проф. Е.М. Семенову за ценные советы и плодотворное обсуждение.



%===============Список литературы==================
\end{fulltext}
\begin{thebibliography}{99}

\RBibitem{Lorentz}
\by G.\,G.~Lorentz
\paper A contribution to the theory of divergent sequences
\vol 80
\jour Acta Math.
\yr 1948
\pages 167--190
\crossref{https://doi.org/10.1007/BF02393648}

%Статья в трудах конференции
\RBibitem{Alekhno_ac0}
\by Е.\,А.~Алехно
\paper Некоторые специальные свойства функционалов Мазура. II
\inbook Аналитические методы анализа и дифференциальных уравнений -- 2006
\publ Труды Института математики НАН Беларуси
\publaddr Минск
\yr 2006
\vol 2
\pages 17--23

\RBibitem{SemSukUsa14}
\by Е.~М.~Семeнов, Ф.~А.~Сукочев, А.~С.~Усачев
\paper Геометрические свойства множества банаховых пределов
\jour Изв. РАН. Сер. матем.
\yr 2014
\vol 78
\issue 3
\pages 177--204
\mathnet{http://mi.mathnet.ru/izv8092}
\crossref{https://doi.org/10.4213/im8092}
\mathscinet{http://www.ams.org/mathscinet-getitem?mr=3241823}
\zmath{https://zbmath.org/?q=an:06323426}
\adsnasa{http://adsabs.harvard.edu/cgi-bin/bib_query?2014IzMat..78..596S}
%\elib{http://elibrary.ru/item.asp?id=21826423}
\transl
\jour Izv. Math.
\yr 2014
\vol 78
\issue 3
\pages 596--620
\crossref{https://doi.org/10.1070/IM2014v078n03ABEH002700}
%\isi{http://gateway.isiknowledge.com/gateway/Gateway.cgi?GWVersion=2&SrcApp=PARTNER_APP&SrcAuth=LinksAMR&DestLinkType=FullRecord&DestApp=ALL_WOS&KeyUT=000338994800009}
%\scopus{http://www.scopus.com/record/display.url?origin=inward&eid=2-s2.0-84903452369}



\RBibitem{VZMS}
\by Н.\,Н.~Авдеев, Е.\,М.~Семенов
\paper Об асимптотических свойствах оператора Чезаро
\inbook Материалы Международной конференции «Воронежская зимняя математическая школа С. Г. Крейна – 2018»
\publ ИПЦ «Научная книга»
\publaddr Воронеж
\yr 2018
\pages 107--109
%\elib{https://elibrary.ru/item.asp?id=32879455}
%Команда \elib почему-то не работает. Процитируйте её правильно, пожалуйста, если я где-то напутал(((



\end{thebibliography}


\end{document}
